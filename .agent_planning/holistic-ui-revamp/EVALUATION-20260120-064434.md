# CC-Viz Frontend Evaluation: Holistic UI Revamp

**Date**: 2026-01-20
**Purpose**: Fresh evaluation for sprint planning of 5 front-end showcase designs
**Verdict**: **CONTINUE** - All 6 epics ready for implementation

---

## Executive Summary

The CC-Viz frontend has a **strong foundation** for implementing the holistic UI revamp:

| Category | Status | Notes |
|----------|--------|-------|
| Theme Tokens | 60% | Dark mode done, light mode & scales missing |
| Chart Components | 95% | StatCard, HourlyUsageChart, ModelBreakdownChart ready |
| Layout Components | 80% | ResizablePanel/PanelGroup excellent, Drawer/Timeline missing |
| Base UI Components | 0% | Button, Input, Card, Badge, Tabs all need building |
| Data Fetching | 100% | All API hooks ready via TanStack Query |
| Virtualization | 100% | @tanstack/react-virtual integrated |
| Routing | 100% | TanStack Router established |

**Key Finding**: The Design System epic (cc-viz-3j1) is the **critical path**. Once base components are built, all 5 feature epics can proceed in parallel.

---

## What Exists (Reusable)

### Charts (components/charts/)
- **StatCard** - Metric display with trend indicator (reusable everywhere)
- **HourlyUsageChart** - Recharts AreaChart with model colors
- **ModelBreakdownChart** - Recharts PieChart/donut
- **WeeklyUsageChart** - Recharts BarChart with formatTokens()
- **PerformanceChart** - Latency percentiles

### Layout (components/layout/)
- **ResizablePanel/PanelGroup** - Perfect for Cockpit Split View
- **Sidebar** - Navigation with collapse
- **GlobalDatePicker** - Date range context
- **PageHeader** - Title + actions pattern

### Patterns
- **Requests page** - Virtualized list + detail split view
- **Conversations page** - Search + list + detail pattern
- **Dashboard page** - Stat cards grid + charts

### API Hooks (lib/api.ts)
- All 40+ hooks ready: sessions, conversations, requests, todos, plans, stats, extensions
- Smart caching with staleTime
- Search with FTS5 (unified search completed)

### Dependencies
- React 19.2.0, TanStack Query 5.90, TanStack Router 1.143
- Tailwind v4.1.18, CVA 0.7.1 for variants
- Recharts 3.6.0, @tanstack/react-virtual 3.13

---

## What's Missing (To Build)

### Design System Epic Requirements

1. **Design Tokens** (cc-viz-3j1.1)
   - Light mode CSS variables
   - Spacing scale (2px → 48px)
   - Typography scale (12px → 32px)
   - Shadow/elevation system
   - Border radius standards
   - Transition timings

2. **Base Components** (cc-viz-3j1.2)
   - Button (primary, secondary, ghost, danger, sizes)
   - Input (text, search, number)
   - Card (generic container)

3. **Data Visualization** (cc-viz-3j1.3)
   - Enhance StatCard with design tokens
   - ChartWrapper for consistent styling

4. **Layout Components** (cc-viz-3j1.4)
   - DataList (virtualized + sort + filter)
   - DetailDrawer (slide-out panel)
   - Timeline (vertical event display)

5. **Theme System** (cc-viz-3j1.5)
   - ThemeProvider context
   - dark/light switching
   - localStorage persistence

6. **Utilities** (cc-viz-3j1.6)
   - SearchInput with debounce
   - CopyableId with truncation
   - StatusBadge variants
   - Tabs component

---

## Confidence Assessment

| Epic | ID | Confidence | Rationale |
|------|----|------------|-----------|
| Design System | cc-viz-3j1 | **HIGH** | Clear component list, CVA patterns exist, Tailwind v4 ready |
| Mission Control | cc-viz-tqy | **HIGH** | Dashboard pattern exists, StatCard ready, just needs enhancement |
| Session Timeline | cc-viz-bjd | **HIGH** | Split view pattern exists, ConversationList pattern available |
| Cockpit Split | cc-viz-0xr | **HIGH** | ResizablePanel perfect, Requests page is template |
| Token Economics | cc-viz-89o | **HIGH** | All chart types exist, formatTokens() ready |
| Extension Workshop | cc-viz-9dx | **HIGH** | ExtensionsHub exists, add DetailDrawer + filtering |

**All 6 epics have HIGH confidence** - the work is well-defined and patterns exist.

---

## Dependency Graph

```
Design System (cc-viz-3j1)
    ↓ must complete 80%+ ↓
    ├── Mission Control (cc-viz-tqy) ─┐
    ├── Session Timeline (cc-viz-bjd) │
    ├── Cockpit Split (cc-viz-0xr)    ├── Can run in PARALLEL
    ├── Token Economics (cc-viz-89o)  │
    └── Extension Workshop (cc-viz-9dx)┘
```

---

## Technical Risks

| Risk | Severity | Mitigation |
|------|----------|------------|
| No base UI components | HIGH | Build in Design System sprint first |
| Timeline component complexity | MEDIUM | Custom HTML/CSS with scroll sync |
| DataList performance with 333K records | MEDIUM | Use @tanstack/react-virtual (already available) |
| Theme switching edge cases | LOW | Test with localStorage + system preference |

---

## Unknowns (Resolved)

1. **Light mode colors** → Will mirror dark mode with inverted values
2. **Component library philosophy** → Continue custom + CVA (consistent with codebase)
3. **Drawer animation** → Use CSS transform + transition (no extra library)
4. **Timeline visualization** → Custom vertical timeline, not Recharts

---

## Verdict: CONTINUE

All ambiguities resolved. Ready to generate sprint plans for all 6 epics.

**Next Step**: Create 6 sprint plans (1 per epic) with acceptance criteria.
