/**
 * Mantine v2 Components
 *
 * This is the shared component library for the v2 dashboard.
 * All v2 pages should import from here, NOT from /components/.
 *
 * v1 pages MAY import from here for backporting.
 */

// Core UI primitives
export * from './core';

// Layout components (AppShell, Navbar, etc.)
export * from './layout';

// Feature-specific components (StatCard, Timeline, etc.)
export * from './features';

// Chart wrappers (Recharts with Mantine styling)
export * from './charts';
